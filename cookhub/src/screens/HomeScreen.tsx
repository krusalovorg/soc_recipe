import React, { useEffect, useState, useContext } from 'react';
import { ScrollView, View, Text, ImageBackground, Animated, StyleSheet, TouchableHighlight, Dimensions } from 'react-native';

const HomeScreen = ({ navigation }: any) => {

    return (
        <ScrollView>
            <Text>home</Text>
        </ScrollView>
    )
}

export default HomeScreen;